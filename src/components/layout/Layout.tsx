import { Wrapper } from "./style";
import BlogHeader from "../blogHeader/BlogHeader";

const Layout = () => {
  return (
    <Wrapper>
      <BlogHeader />
    </Wrapper>
  );
};

export default Layout;
